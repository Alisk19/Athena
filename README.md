# Athena Web (Docker-ready)

A minimal Node.js + Express app serving a static frontend inspired by the Athena UI screenshot.

## Run locally
```bash
npm install
npm run dev
# open http://localhost:3000
```

## Run with Docker
```bash
docker compose up --build
# open http://localhost:3000
```

